const initialStateProfile = {

}

export const profileReducer = (state: initialProfileType = initialStateProfile, action: actionsProfileType) => {
    switch (action.type) {
        default:
            return state
    }
}


//types
export type initialProfileType = {

}
export type actionsProfileType = any